import WeatherDetail from "./weather-detail.component";

const WeatherForecast = (props: any) => {
  const { weather, onFavClick } = props;
  return <div className="w-[40rem] h-[25rem] bg-white/30"></div>;
};
export default WeatherForecast;
